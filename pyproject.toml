[project]
name = "codiness"
version = "0.1.0"
description = "Assess code with Laya"
requires-python = ">=3.12"
dependencies = [
 "laya>=0.3.20",
 "marimo>=0.25.0",
]

[project.scripts]
codiness = "codiness:main"

[build-system]
requires = ["uv_build>=0.11.0,<0.13.0"]
build-backend = "uv_build"
